# Light Gallery


## Notes
- update partial files `header.html` and `footer.html`
- init script is located in `footer.html` 
- copy `images` and `fonts` directory to static to ensure bundled css loads the assets
- Lightgallery only works when `image` shortcode is used. Will not work on `figure` shortcode
- still need to test compatibility with blowfish gallery shortcode. 

