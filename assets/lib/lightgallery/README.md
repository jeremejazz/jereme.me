# Light Gallery


## Notes
- update partial files `header.html`, `footer.html`, `assets.html`, and `theme.js`
- copy `images` and `fonts` directory to static to ensure bundled css loads the assets
- Lightgallery only works when `image` shortcode is used. Will not work on figure. 
- still need to test compatibility with blowfish gallery shortcode. 

